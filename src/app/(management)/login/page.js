import { Button } from "@/components/ui/button";

export default function Login() {
  return (
    <main className={"container mx-auto min-h-[calc(100vh-249.27px)]"}>
      <Button variant="destructive">Bouton</Button>
    </main>
  );
}
