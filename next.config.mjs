// Configurations de next.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    // Config langage
    locales: ["fr", "en"],
    defaultLocale: "fr",
  },
};

export default nextConfig;
